<Window x:Class="Anno1800Planner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Anno1800Planner"
        Title="Anno 1800 Planner" Height="600" Width="1000">
    <Grid>
        <!-- Main layout with content on the left and menu on the right -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>

        <!-- Content Area -->
        <ContentControl Content="{Binding CurrentContent}" />

        <!-- Navigation Menu -->
        <StackPanel Grid.Column="1" Background="#EEE">
            <TextBlock Text="Navigation" FontWeight="Bold" Margin="0 0 0 0" />
            <Button Content="Game Data" Command="{Binding NavigateToResources}" Margin="0 10 0 0" />
            <Button Content="Buildings" Command="{Binding NavigateToBuildings}" Margin="0 2 0 0" />
            <Button Content="ResidentTiers" Command="{Binding NavigateToResidentTiers}" Margin="0 2 0 0" />
            <Button Content="Production Chains" Click="ProductionChains_Click" Margin="0 10 0 0"/>
            <Button x:Name="SaveButton" Content="Save" Click="Save_Click" Margin="0 10 0 0"/>
            <Button Content="Load" Click="Load_Click" Margin="0 2 0 0"/>

            <TextBlock Text="Plans" FontWeight="Bold" Margin="0 10 0 5"/>
            <Button Content="+ Add Plan" Click="AddPlan_Click" Margin="0 5 0 10"/>

            <ListBox x:Name="PlanListBox" DisplayMemberPath="Name" SelectionChanged="PlanSelected" />
        </StackPanel>
    </Grid>
</Window>
